/**
 * ----------------------------------
 *              ACTORS
 * ----------------------------------
 * @author yourself
 * In this file you will find:
 * - stored classes for actors page
 * - instances of object in the Immediately-invoked Function of actors page
 * /

/**
 * @Class 
 * General class for the actors page
 */
class Actors {
  //to do actor class
}

/**
 * @Class 
 * Add event to DOM elements for actors page
 */
class ActorsListeners {
   //to do actor event handlers
}

/**
 * @Class 
 * Ajax methods for actors page
 */
class ActorsAjax {
  //to do jquery ajax request 
}

/**
 * IIF (Immediately-invoked Function)
 * This the function scop where all the script will execute
 * It prevent of collision with other script
 * listeners and ajax are object inside the scop of movies
 * templates, messageAlert and clear are shared object in the global scop from common.js file
 */
(function (templates, messageAlert, clear) {
    //to do IIF content
})(templates, messageAlert, clear)

